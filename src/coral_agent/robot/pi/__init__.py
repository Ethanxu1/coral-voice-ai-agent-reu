"""Pi-side robot package (ROS nodes + robot_server). Most modules require the
robot's ROS environment, but ``nodes.motions`` is pure pose data and is reused
by the Mac sim demo (see server.py /motion)."""
