"""CORAL Mac-side package — roslibpy clients (director, audio, speaker) that
connect to the Pi's rosbridge, plus the reused motion-planning modules
(primitives, validation, angle conversion, router prompt)."""
